GR. 7+16
DIM A(160) byte
OFFSET=0
screen=PEEK($59)*256+PEEK($58)
OPEN #1,4,0,"D2:ABEJUARE.BMP"
BGET #1,ADR(A),154-32+40
WHILE ERR()=1
    BGET #1,ADR(A),160
    FOR I=0 TO 159
        dbyte=A(I)
        POKE screen+I+OFFSET,dbyte
    NEXT I
    OFFSET=OFFSET+160
WEND
